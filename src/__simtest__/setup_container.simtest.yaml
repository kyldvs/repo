name: "setup_container"

desc: "Verifies the pod_clone environment delivers a clean, ready working tree"

environment: pod_clone
tags: [container, slow]

test:
  - assert: dir_exists
    input:
      path: node_modules

  - assert: is_clean
