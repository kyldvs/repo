name: "meta_isolation_a"

desc: "Half of an isolation pair: succeeds only when the runner started ctx.cwd at the repo root"

environment: local
tags: [meta, fast]

test:
  - assert: dir_exists
    input:
      path: src
