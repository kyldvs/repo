# Todo

- Add `src/repo` wrappers for all built-in libraries, starting with `node:fs`. Goal: ban stdlib imports outside `src/repo`, enable DI for testing, tighten types, and add guardrails around stdlib usage.
